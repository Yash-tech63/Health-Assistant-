const router = require('express').Router(); const c = require('../controllers/queueController'); const auth = require('../middleware/authMiddleware'); router.use(auth.verifyToken); router.post('/check-in/:appointmentId', auth.authorizeRoles('patient'), c.checkIn); router.get('/status/:appointmentId', auth.authorizeRoles('patient'), c.status); router.post('/next', auth.authorizeRoles('doctor'), c.next); router.post('/complete/:appointmentId', auth.authorizeRoles('doctor'), c.complete); module.exports = router;
