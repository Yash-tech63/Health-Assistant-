const router = require('express').Router(); const c = require('../controllers/clinicalController'); const auth = require('../middleware/authMiddleware'); router.use(auth.verifyToken); router.post('/', auth.authorizeRoles('doctor'), c.createRecord); router.get('/patient/:patientId', c.recordsForPatient); router.get('/:id', c.getRecord); router.put('/:id', auth.authorizeRoles('doctor', 'admin'), c.updateRecord); module.exports = router;
